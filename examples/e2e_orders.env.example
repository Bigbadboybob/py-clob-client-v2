# Required
PK=0x...
TOKEN_ID=

# Optional: counterparty wallet for steps 2–9 (market/marketable orders)
PK2=

CHAIN_ID=80002
CLOB_API_URL=

# Optional: order size in USDC (defaults to market's minimum_order_size)
ORDER_SIZE=

# Required for on-chain setup (token approvals, PMCT wrapping, collateral split)
RPC_URL=
COLLATERAL_TOKEN_ADDRESS=
WRAPPER_ADDRESS=

# Optional: bytes32 builder code to run builder-code order tests (steps 8–9)
BUILDER_CODE=
